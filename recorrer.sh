#!/usr/bin/bash
Documento ="$1"
while read -r linea; do
    echo "$linea"
done < "$Documento"
for $linea in $frase; do
    if [ "$frase" == "a" ]; then
        n=1
        while [ "linea" = "x" ]; do
            array.legth replace "x" to contain
            for "array" in $container; do
            echo "$array"
    fi
        done    
done